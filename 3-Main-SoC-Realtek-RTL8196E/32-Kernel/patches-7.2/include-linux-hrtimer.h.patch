--- a/include/linux/hrtimer.h
+++ b/include/linux/hrtimer.h
@@ -271,6 +271,10 @@
 extern ktime_t hrtimer_get_next_event(void);
 extern ktime_t hrtimer_next_event_without(const struct hrtimer *exclude);
 
+/* Collect up to @max .function pointers of active hrtimers on this CPU
+ * (earliest-expiry first); returns the count. Watchdog panic notifier. */
+int hrtimer_collect_pending_fns(void **out, int max);
+
 extern bool hrtimer_active(const struct hrtimer *timer);
 
 /**
