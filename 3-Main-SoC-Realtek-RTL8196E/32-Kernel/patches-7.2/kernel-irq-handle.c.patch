--- a/kernel/irq/handle.c
+++ b/kernel/irq/handle.c
@@ -20,6 +20,12 @@
 
 #include "internals.h"
 
+#if defined(CONFIG_RTL8196E_IMEM) && defined(CONFIG_RTL8196E_IMEM_DEFAULT_PLACEMENT)
+#define __iram_hotpath __attribute__((section(".iram")))
+#else
+#define __iram_hotpath
+#endif
+
 #ifdef CONFIG_GENERIC_IRQ_MULTI_HANDLER
 void (*handle_arch_irq)(struct pt_regs *) __ro_after_init;
 #endif
@@ -182,7 +188,16 @@
 	}
 }
 
-irqreturn_t __handle_irq_event_percpu(struct irq_desc *desc)
+/*
+ * Deliberately not __iram_hotpath, unlike the two callers below.  This
+ * function carries a static-branch site, and the on-chip window is a
+ * non-coherent copy taken once at boot: a key toggled later would be
+ * patched in the SDRAM shadow the CPU no longer reads.  The kernel
+ * refuses the patch and warns, or -- if nothing ever toggles that key --
+ * says nothing at all and leaves the branch wrong forever.  Neither is
+ * acceptable, so the function stays in ordinary text.
+ */
+irqreturn_t __handle_irq_event_percpu(struct irq_desc *desc)
 {
 	irqreturn_t retval = IRQ_NONE;
 	unsigned int irq = desc->irq_data.irq;
@@ -239,7 +254,7 @@
 	return retval;
 }
 
-irqreturn_t handle_irq_event_percpu(struct irq_desc *desc)
+irqreturn_t __iram_hotpath handle_irq_event_percpu(struct irq_desc *desc)
 {
 	irqreturn_t retval;
 
@@ -252,7 +267,7 @@
 	return retval;
 }
 
-irqreturn_t handle_irq_event(struct irq_desc *desc)
+irqreturn_t __iram_hotpath handle_irq_event(struct irq_desc *desc)
 {
 	irqreturn_t ret;
 
